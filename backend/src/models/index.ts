/**
 * Domain model re-exports for convenience.
 * Prisma remains the source of truth for persistence models.
 */
export type { User, Product, Order, OrderItem, Role, OrderStatus } from '@prisma/client';
