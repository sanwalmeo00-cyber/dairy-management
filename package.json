{
  "name": "dairy-management",
  "version": "1.0.0",
  "private": true,
  "description": "Dairy management full-stack Next.js application",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "bun run --cwd frontend dev",
    "build": "bun run --cwd frontend build",
    "start": "bun run --cwd frontend start",
    "lint": "bun run --cwd frontend lint",
    "install:all": "bun install --cwd frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
